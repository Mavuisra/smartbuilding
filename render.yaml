# Déploiement cloud — LWS (PHP + MySQL)

L'API cloud est désormais **`sbms-cloud/`** (PHP 8.1, Clean Architecture, Slim 4).

L'ancienne stack Django/Render (`smartbuilding-web/`, PostgreSQL Render) a été retirée.

Voir **`sbms-cloud/README.md`** pour le déploiement sur hébergement mutualisé LWS.
